opencv-python>=4.8.0
paddlepaddle>=2.5.0  # PaddlePaddle framework for PaddleOCR
paddleocr>=2.7.0  # PaddleOCR for better handwriting recognition
numpy>=1.24.0
Pillow>=10.0.0
fuzzywuzzy>=0.18.0  # Optional but recommended for better name matching
python-Levenshtein>=0.12.0  # Optional but recommended for faster fuzzywuzzy
openai>=1.0.0  # For embeddings and LLM name correction
python-dotenv>=1.0.0  # For loading .env file
# pytesseract>=0.3.10  # Deprecated - using PaddleOCR instead

# Vector database support
scikit-learn>=1.3.0  # For cosine similarity in local vector storage (required)
# Optional vector DBs (not needed for local mode):
# pinecone-client>=3.0.0  # For Pinecone vector DB (optional)
# chromadb>=0.4.0  # For ChromaDB vector DB (optional)
# weaviate-client>=3.24.0  # For Weaviate vector DB (optional)

# Database drivers (install only what you need)
# SQLite is built-in to Python, no install needed
# psycopg2-binary>=2.9.0  # For PostgreSQL
# mysql-connector-python>=8.0.0  # For MySQL
# pymongo>=4.0.0  # For MongoDB

