# Simplified Scanner Requirements

# Core dependencies (required on all platforms)
opencv-python>=4.5.0
python-dotenv>=0.19.0

# GoPiGo dependencies (auto-installed on Raspberry Pi only)
# These are optional - scanner will auto-detect platform
# picamera2>=0.3.0
# easygopigo3>=1.0.0


